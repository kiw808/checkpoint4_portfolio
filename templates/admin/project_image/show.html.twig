{% extends 'admin/base.html.twig' %}

{% block title %}ProjectImage{% endblock %}

{% block body %}
    <h1>ProjectImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_image.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ project_image.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ project_image.updatedAt ? project_image.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_project_image_index') }}">back to list</a>

    <a href="{{ path('admin_project_image_edit', {'id': project_image.id}) }}">edit</a>

    {{ include('admin/project_image/_delete_form.html.twig') }}
{% endblock %}
