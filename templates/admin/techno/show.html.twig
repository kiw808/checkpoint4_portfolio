{% extends 'admin/base.html.twig' %}

{% block title %}Techno{% endblock %}

{% block body %}
    <h1>Techno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ techno.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ techno.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ techno.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ techno.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ techno.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_techno_index') }}">back to list</a>

    <a href="{{ path('admin_techno_edit', {'id': techno.id}) }}">edit</a>

    {{ include('admin/techno/_delete_form.html.twig') }}
{% endblock %}
